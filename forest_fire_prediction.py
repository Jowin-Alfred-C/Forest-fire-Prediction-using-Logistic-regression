# -*- coding: utf-8 -*-
"""forest fire prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1wlYnFWYZFVEdTI-SH6KX8rD-kJOV6UPp
"""

#importing necessary libraries
import numpy as np
import pandas as pd
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import train_test_split
import matplotlib.pyplot as plt
import warnings
import pickle
warnings.filterwarnings("ignore")

#loading dataset
data = pd.read_csv("/content/Forest_fire.csv")

data.head()

data.tail()

#checking null values
data.isnull().sum()

#getting info about dataset
data.shape

data.describe()

data.info()

data.mean()

#getting insights about the data
plt.scatter(x =data.Oxygen, y = data.Temperature)
plt.xlabel("oxygen")
plt.ylabel("temperature")
plt.show()

plt.scatter(x =data.Oxygen, y = data.Humidity)
plt.xlabel("oxygen")
plt.ylabel("humidity")
plt.show()

#converting into array
data = np.array(data)

#split dataset
X = data[1:, 1:-1]
y = data[1:, -1]
y = y.astype('int')
X = X.astype('int')
print(X,y)

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=0)

#select the model and train it
log_reg = LogisticRegression()

log_reg.fit(X_train, y_train)

log_reg.coef_

log_reg.intercept_

#saveing the model
pickle.dump(log_reg,open('model.pkl','wb'))